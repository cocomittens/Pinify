// export const getPins = (state, boardId) => {
//     let result = [];
//     let pinsBoards = state.entities.pins;
//     for (let id in pinsBoards) {
//         if (pinsBoards[id].board_id === boardId) { result.push(pinsBoards[id].pin_id); }
//     }

//     return result;
// };

